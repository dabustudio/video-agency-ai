# memory

Persistent and session-scoped memory storage for agents.

Stores context, past decisions, user preferences, project history, and any information that needs to persist across agent runs or sessions.

May include vector store integrations, local file-based memory, or structured databases depending on the use case.
